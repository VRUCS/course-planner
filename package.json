{
  "name": "entekhab-vahed-dev-tools",
  "private": true,
  "description": "Dev-only tooling (lint). The site itself runs without any build step.",
  "scripts": {
    "lint": "eslint",
    "test": "node tests/js/domain.test.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
